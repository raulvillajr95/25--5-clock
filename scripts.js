let sessionId = document.getElementById('session-length')
let sessionLenNum = sessionId.innerText

let breakId = document.getElementById('break-length')
let breakLenNum = breakId.innerText

console.log(breakLenNum)